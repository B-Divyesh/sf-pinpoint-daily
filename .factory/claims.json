[
  {
    "id": "demo-isolation",
    "claim": "Try it with sample data opens a separate demo that never reads or changes ordinary game data",
    "where": "landing action, demo banner, README",
    "test": "npm run test:browser -- --grep @claim:demo-isolation",
    "sandbox": "Fresh browser contexts; seed contradictory ordinary run, best, completed-date, and sound values; enter through /demo and /?demo=1; assert independent demo defaults and unchanged ordinary storage."
  },
  {
    "id": "demo-focus",
    "claim": "Play the sample course moves focus to the demo course",
    "where": "demo first-screen action",
    "test": "npm run test:browser -- --grep @claim:demo-focus",
    "sandbox": "Fresh /demo; select Play the sample course and assert the course heading receives focus."
  },
  {
    "id": "shared-daily-course",
    "claim": "Everyone gets the same three-hole course for a UTC day",
    "where": "landing page, footer, README",
    "test": "npm run test:browser -- --grep @claim:shared-daily-course",
    "sandbox": "Two fresh browser contexts use a fixed UTC clock, enter through /demo, switch to the daily course, and compare all three rendered boards after identical advancement and reload."
  },
  {
    "id": "visible-prediction",
    "claim": "A dotted path previews a shot before release",
    "where": "game board, How it works, README",
    "test": "npm run test:browser -- --grep @claim:visible-prediction",
    "sandbox": "Fresh /demo; measure the rendered cyan path, then compare prediction points with first-shot and later bumper-collision simulation from the exact live snapshot."
  },
  {
    "id": "visible-course-elements",
    "claim": "Wind, walls, and the moving bumper are visible during play",
    "where": "How it works, README",
    "test": "npm run test:browser -- --grep @claim:visible-course-elements",
    "sandbox": "Fresh /demo; count the cream wall pixels, identify the coral bumper, observe its movement during a shot, advance, and assert the wind label."
  },
  {
    "id": "five-shot-limit",
    "claim": "Each hole allows five shots",
    "where": "landing facts, game board, README",
    "test": "npm run test:browser -- --grep @claim:five-shot-limit",
    "sandbox": "Fresh /demo; take and reset exactly five misses, then assert advancement, total count, and the next hole's zero count."
  },
  {
    "id": "run-persistence",
    "claim": "The current run persists in local browser storage",
    "where": "privacy page and README",
    "test": "npm run test:browser -- --grep @claim:run-persistence",
    "sandbox": "Fresh /demo; reach hole two, take a shot, reload, and verify the same hole and shot count are restored."
  },
  {
    "id": "sound-setting",
    "claim": "Sound effects work after a gesture and the sound setting persists",
    "where": "game sound control, README",
    "test": "npm run test:browser -- --grep @claim:sound-setting",
    "sandbox": "Fresh /demo with a recorded Web Audio fixture; enable sound, observe a tone, reload, and observe the saved setting and shot tone."
  },
  {
    "id": "local-privacy",
    "claim": "The game has no accounts, ads, analytics, cookies, or game-data requests",
    "where": "privacy section, privacy page, README",
    "test": "npm run test:browser -- --grep @claim:local-privacy",
    "sandbox": "Fresh /demo; complete win and loss runs; assert no account or ad UI, beacons, WebSockets, EventSource, cookies, query strings, request bodies, or requests outside the exact document/static-asset GET allowlist."
  },
  {
    "id": "input-methods",
    "claim": "The game supports drag, labelled controls, Arrow-key aim and power, Enter to shoot, R to reset, and Escape to pause",
    "where": "game instructions, README",
    "test": "npm run test:browser -- --grep @claim:input-methods",
    "sandbox": "Fresh /demo; exercise Escape pause, zero-length and non-zero drags, ArrowLeft/ArrowRight aim, ArrowUp/ArrowDown power, Enter fire, R reset, and labelled controls; assert visible feedback and shot state."
  },
  {
    "id": "distinct-outcomes",
    "claim": "Sinking all three cups wins; missing a hole produces a distinct loss",
    "where": "How it works, end screens, README",
    "test": "npm run test:browser -- --grep @claim:distinct-outcomes",
    "sandbox": "Two fresh /demo contexts run deterministic three-shot win and fifteen-miss scripts to different end screens."
  },
  {
    "id": "best-score",
    "claim": "A winning best score persists in local browser storage",
    "where": "game results, privacy page, README",
    "test": "npm run test:browser -- --grep @claim:best-score",
    "sandbox": "Fresh /demo; complete the deterministic three-shot win, inspect the saved best, reload, and verify the winning result remains."
  },
  {
    "id": "restart-reset",
    "claim": "Play again starts a fresh run and keeps the saved best score, completed date, and sound setting",
    "where": "win and loss screens",
    "test": "npm run test:browser -- --grep @claim:restart-reset",
    "sandbox": "Fresh /demo; enable sound, complete the deterministic three-shot win, select Play again, and assert a fresh hole-one run while saved progress and sound remain."
  },
  {
    "id": "completed-date-persistence",
    "claim": "A completed course date persists in local browser storage",
    "where": "privacy page",
    "test": "npm run test:browser -- --grep @claim:completed-date-persistence",
    "sandbox": "Fresh /demo; complete the deterministic win, assert the sample date is stored, reload, and assert it remains stored."
  },
  {
    "id": "clear-local-score-history",
    "claim": "Clear local score history asks for confirmation, cannot be undone, and removes only the best score and completed dates",
    "where": "game control and privacy page",
    "test": "npm run test:browser -- --grep @claim:clear-local-score-history",
    "sandbox": "Fresh /demo; seed every saved field, cancel once, then confirm; assert the warning, permanent removal after reload, no Undo control, and preservation of only the current run and sound."
  },
  {
    "id": "storage-removal",
    "claim": "Clearing this site's browser data deletes all saved real and demo game data",
    "where": "privacy page",
    "test": "npm run test:browser -- --grep @claim:storage-removal",
    "sandbox": "Fresh browser context; seed full ordinary and demo saves, clear origin localStorage, reload both modes, and assert every saved field remains absent with default game state."
  },
  {
    "id": "result-sharing",
    "claim": "Completed win and loss screens copy the date, cups, and shot total without course geometry",
    "where": "game results and README",
    "test": "npm run test:browser -- --grep @claim:result-sharing",
    "sandbox": "Fresh /demo win and loss contexts with a recorded Clipboard fixture; assert exact copied text for each outcome."
  },
  {
    "id": "frame-rate-target",
    "claim": "The game uses fixed 60 Hz simulation and targets 60 rendered frames per second on a mid-range phone",
    "where": "README",
    "test": "npm run test:browser -- --grep @claim:frame-rate-target",
    "sandbox": "Fresh /demo at 390x844 under 4x CPU throttling; assert the 60 Hz simulation marker and measure 120 rendered frames at 55-65 fps."
  },
  {
    "id": "free-play",
    "claim": "The complete game is free to play without an account or payment step",
    "where": "landing facts, terms, README",
    "test": "npm run test:browser -- --grep @claim:free-play",
    "sandbox": "Fresh /demo; complete all three holes, checking every stage and the result screen for playable controls and no sign-in or payment gate."
  },
  {
    "id": "online-first-load",
    "claim": "Internet access is needed to open the game on a fresh first visit",
    "where": "landing facts",
    "test": "npm run test:browser -- --grep @claim:online-first-load",
    "sandbox": "Fresh browser context with no cache; set the context offline before opening /demo and assert navigation fails, then reconnect and assert the playable game opens."
  },
  {
    "id": "static-deploy",
    "claim": "The production build creates static files with route rewrites and security headers",
    "where": "README deployment instructions",
    "test": "npm run test:browser -- --grep @claim:static-deploy",
    "sandbox": "Build and serve dist through the Static Web Apps emulator; check every route's status, metadata, legal links, headers, focus, 404 shell, and revalidating fixed-name icons."
  }
]
